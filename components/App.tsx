/**
 * ARCHITECTURAL REFACTOR (v7.1.1)
 * This component was a duplicate of the main App.tsx and is no longer maintained.
 * To prevent accidental use and ensure a single source of truth, it now re-exports
 * the primary App component from the root directory.
 */
export { App } from '../App.tsx';
